- Drop a `vendor_boot.img` into the `input` folder.

- Run `patcher.sh`.

- You should now have a file called `magisk_patched_vendor_boot.img`.

- Flash or test boot the newly created `magisk_patched_vendor_boot.img`.

- ENJOY!!
